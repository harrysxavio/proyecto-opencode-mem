# Plugins

Plugin files in this repo are templates. They are not active runtime plugins until a user reviews and installs them manually or through a future approved installer.

Phase 0 provides only `engram.template.ts` as conceptual guidance.
