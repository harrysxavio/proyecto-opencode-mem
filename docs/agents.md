# Agents

The kit defines agent templates under `agents/`.

| Directory | Purpose |
|---|---|
| `agents/manager/` | Primary Manager template |
| `agents/sdd/` | SDD subagent templates |

Templates are reviewed before installation and never installed automatically in Phase 0.
