# Agents

This directory contains sanitized agent templates. They are examples for reviewed installation, not copies of a real runtime.

- `manager/` contains the primary Manager template.
- `sdd/` contains SDD subagent templates.
