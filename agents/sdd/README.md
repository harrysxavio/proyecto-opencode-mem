# SDD Subagent Templates

These are sanitized starter templates for SDD phases. They are not copied from a real runtime.

Each template includes:

- purpose
- when to use
- when not to use
- inputs
- outputs
- `SUBAGENT_RESULT`
- no secrets
- no personal paths

`sdd-init.template.md` also includes `SDD_INIT_PACKET`.
