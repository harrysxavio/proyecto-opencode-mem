# Manager Template

The Manager is the single primary agent. It classifies requests, chooses the safest workflow, delegates SDD phases, controls memory persistence, applies Ponytail guidance for code tasks, treats gentle-ai as alignment-only, and synthesizes final responses.

Use `manager.template.md` as a starting point, then adapt it to the target machine after review.
